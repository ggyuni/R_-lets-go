a <- matrix(1:20,4,5) ## a에 5개의 행, 4개의 열로 1에서 20까지의 내용을 출력함

b <- matrix(21:40,4,5) ## b에 에 5개의 행, 4개의 열로 21에서 40까지의 내용을 출력함

a ## 내용을 출력함

b ## 내용을 출력함


2*a ## 매트릭스 a에 저장된 값들에 2를 곱하기


a+b ## 매트릭스 a와b에 저장된 값을 더함

