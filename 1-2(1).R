3 + (4*5) ## 3더하기 (4곱하기5)의 값이 도출됨


A <- 51:80 ## 51:80은 51~80사이 까지의 숫자를 의미함
print(A)  ## A값을 도출시키는 명령어


3 + (4 * 5 ## )를 빠트렸을 때 결과값은 실행값뒤에 +가 붙어 나옴
     


print(B) ## 이와 같이 도출할 수없는 값을 적었을 때에는 에러메서지가 도출됨


2+3 ## 5라는 값이 나옴


(3+6)*8 ## 72의 값이 도출


2^3 ## 2의 세제곱을 의미함/8의 값이 도출



7+4 ## 11의 값이 도출


# 2^3 ## (#)은 주석문으로 간주하여 실행되지 않음