dim(iris) ## 행과 열의 개수 보이기, 행은 150 열은 5로 결과값이 나타남
nrow(iris) ## 행의 개수 보이기, 행이 150임을 알 수 있음
ncol(iris) ## 열의 개수 보이기 열이 5임을 알 수 있음
colnames(iris) ## 열의 이름 보이기, names() 함수와 결과 동일, 5개의 열이름을 확인 할 수 있음
head(iris) ## 데이터셋의 앞부분 일부 보기
tail(iris) ## 데이터셋의 뒷부분 일부 보기
